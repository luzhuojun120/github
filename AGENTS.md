# AGENTS.md — 进食检测比赛

> 智能手表传感器进食检测算法项目（全国大学生生物医学工程创新设计竞赛 · 智能穿戴与运动健康技术赛道）

## 项目定位
基于智能手表 IMU/传感器数据的进食动作检测算法。定位：练手 + 考研作品集，目标院校深大/南方医 BME，目标企业联影。

## 怎么跑起来
- 数据集放 `data/`（已 gitignore，不提交）
- 算法代码写 `src/`，探索练习放 `notebooks/`
- 依赖清单：`requirements/requirements.txt`（baseline 已建，未提交）
- 运行环境：Python 3.13

## 技术栈
Python · （待定：sklearn / PyTorch 视数据集规模与任务而定）

## 目录约定
| 路径 | 用途 |
|------|------|
| `data/` | 数据集，本地保留不上传 |
| `src/` | 算法代码 |
| `notebooks/` | 探索性练习 |
| `requirements/` | 依赖清单 |

## 当前状态（2026-09-21 更新 · 距 9-30 提交 9 天）
- ⚠️ **报名待提交** — 截止 **10-20**（2026-09-20 官方公众号"报名延长通知"，原 9-20）；团队信息填写中；提交闭环：生成→邮件通知参赛人→负责人确认→提交，终局「提交」一次性不可逆。指导老师已定：**黄海辉副教授**（2026-09-16 用户确认同意担任）→ 推荐参赛表需打印+导师签字+学院盖章（学院级代章亦可）。⚠️ **作品材料提交原定 9/30，延长通知未提是否顺延 → 暂按 9/30 准备、待官方确认**
- ✅ **数据已全部到手**（2026-09-08）：1165 个 sensorData zip + 288 条 mealinfo 标注（44 人）+ userinfo；重复副本 56 份已隔离待删
- ✅ **端到端管线跑通**（2026-09-10）：data_loader → preprocess → features → detect(RandomForest) → segment_meals → evaluate(IoU+F1)
- ✅ **惯用手分支 detect_dominant 已跑通**（2026-09-21）：`build_imu_matrix`（**6 轴 IMU → 30 维特征**）+ `detect_dominant`（造 y → 训练 → 预测 → segment_meals 合并 → 饭段），真实数据（HNU21026）实测 IoU ≈ 0.996（**仍为「同源训练+预测」的作弊版，分数虚高**）
- ✅ **全量拼接验证通过**（2026-09-21）：35 个 zip 拼成 15,664,936 行连续时间线，跨度 100.45 小时，零丢行
- 🔬 **三个技术发现已建档**（详见 `文件workbuddy/文件1/进食检测_技术发现与报告素材_假菌.md`）：①标签边界膨胀（膨胀量≈1 窗口 4.76s，短段 IoU 掉得狠）②数据组织：饭段会跨 zip 接缝，需改「有交集」判定 ③**ACC 采集断续**：时间戳每 10 行记一次（批头）+ 存在小时级空档，**跨空档滑窗会使特征失真**
- ❌ **PPG 非惯用手分支空白**：`src/detect_nondominant.py` 仍是 8-01 的 TODO 骨架，44 列 PPG 语义未破解
- ✅ 数据字段确认：`mealinfo` 的 **`wearHand.startswith(dietaryHand)` = 惯用手场景**（用户分支）、不等 = 非惯用手（队友分支）——**按行筛，不必按人分**
- 🔴 **致命切分缺陷（2026-09-23 夜实测发现）**：**换手是「成块」的，不是随机的**——同一人连续几顿戴同一只手再整块切换。实测 HNU21004：前半全非惯用手、后半全惯用手 → **「按时间中点切分」必然切出训测场景不对等的实验**。因此：
  - **之前所有基于时间切分的实验结论全部作废**（含 F1=0 与 4.74%→0.58% 那组数）。那 8 倍落差**不是泛化差，是训测场景不同**。
  - 切分必须改为 **按受试者分组（Group Split by Subject）**：同一人所有顿整体归训练或测试，绝不按时间切。
  - ⚠️ **筛惯用手前必须看样本量**：每人惯用手顿中位数仅 2~3 顿，且 **21 人的惯用手顿全挤在同一天** → 时间切分必出 `test_meals=[]` 空测试集（F1=0 是"没数据"，会误导）。
  - 筛选粒度：**按窗口/饭段，不按 zip**（8 顿里 2 顿跨 zip 接缝，同一 zip 可能含两种场景）。
- 📋 **数据体检报告**：`文件workbuddy/文件1/03_竞赛_进食检测/进食检测_数据体检与切分方案_2026-09-23.md`（含脏 ID 清单、规范化规则、修正版 A 方案 A-0~A-3）
- `requirements/requirements.txt` 已建 baseline（numpy/pandas/scipy/scikit-learn/matplotlib）
- 黑马课路线：148-154 Matplotlib 保留，155-185 实战案例延后到军训后
- 📊 时间投入与阶段分析报告：`文件workbuddy/文件1/假菌_进食检测竞赛_时间投入与阶段分析_2026-09-10.html`（口径与优化空间见该文件）

## ⚠️ 数据处理约束（动手前必读 · 2026-09-23 实测）

> 这一节的数字全部为**实测值**，不是估算。改数据处理代码前先看这里，别重算、别踩坑。

### 内存约束：🔴 禁止「全量拼接成一个 DataFrame」

**单片 zip 实测**：`461,190 行 | 24.6 MB | 1.44 s`

| 场景 | 行数 | 内存 | 耗时 |
|---|---|---|---|
| 单人（zip 最多 42 片） | 1,937 万 | **1.0 GB** | 60 s |
| **全量 1157 片一次拼** | **5.34 亿** | **27.8 GB** ❌ | ~28 min |
| 逐个受试者处理（峰值=最大单人） | — | **1.0 GB** ✅ | ~28 min |

- **必须「按受试者逐个处理」**：读一个人 → 提特征 → 释放原始数据 → 下一个
- ⚠️ 本机 32GB，27.8GB 的 DataFrame 在 `pd.concat` 时需**双倍峰值** → 实际突破 50GB，**必爆**

### 缓存粒度：必须按受试者分文件

现有 `cache_X_starts.npz` 是**单文件全量**。改为逐个受试者处理后，缓存必须改为 `cache/<受试者ID>.npz`，否则**改一个人的逻辑会污染全部缓存**。

### 数据源完整性（已核实，无需再查）

- 本地 `data/raw/sensorData/` 实测 **1165 个 zip / 17 GB** —— **下载完整** ✓
- 映射表 1165 条，名册内 1157 条，**差的 8 条 = `6C7`(6 个) + `NAN`(2 个)**，均无标注，本就用不上 ✓
- **同一受试者名下 MAC 数 > 1 的：0 个** → 分组粒度正确，`HNU21026` / `HNU21026J` 是按人干净分开的 ✓

### `src/subjects.py` 调用约定（防标签污染）

```python
subs = load_subjects(RAW, dominant_only=True)
```

- `dominant_only=True` **只过滤「人」不过滤「顿」** —— 返回的 `meals` 仍是该人**全部**顿（惯用手 + 非惯用手）
- **筛顿是 A-2 的职责**；调用时须配合 `is_dominant` 列表使用
- ⚠️ 若误以为 `meals` 已筛过 → 会把非惯用手饭段当正样本标签 → **标签污染**

### 规范化铁律

```python
def norm_id(s):
    return re.sub(r'[\s\-_]', '', str(s).strip().upper())
```

**只去空白/横线/下划线+大写，绝不碰字母数字。**
⚠️ 写成"去掉尾部字母"会把 `HNU21026J` 并进 `HNU21026` —— 那是**两个人**（MAC 不同）。

---

## ✍️ 编码约定与教学沉淀（2026-09-25 · 边写边记）

> 这一节记的是**写代码的习惯**，不是算法知识。每条都对应一次真实踩坑。改代码前扫一眼。

### ① 改错别字：用 IDE 全局替换，别手动找
- 变量名/参数名拼错（如 `uesd` → `used`，共 3~4 处）→ **Ctrl+H 全局替换**，一次改完
- ❌ 手动一处处找 → **漏掉一处就留下"第二真相"**（同一变量两种拼法），比一开始就错更难查
- 适用：改名、改常量、改函数名、改口径参数

### ② 缩进 = 归属（已栽 6 次）
- Python 缩进是**语义**不是排版：这一行"归谁管"
- 高危位置：`return` / `print` / 汇总用 `if` / 累加语句 —— 写这些"收尾动作"时，手最容易跟着上一行的缩进走
- 自查：写完 `for` 块，扫一眼块内有没有"本该在块外"的语句
- 实例：`eval_event` 的 `return` 落进 `for a,b in ranges:` 里（当时 `ranges` 恰好只有 1 个元素 → **侥幸**没发作，属"潜伏 bug"）；`match_count` 的 `if best_v>=thresh` 落进内层 `for i` 里 → 一个预测段被重复计数多次

### ③ 算完东西要交出去（忘 return，已栽 5 次）
- 共同点：函数最后一步是"往容器里塞东西"（`append` / `extend` / `out[x]=…`）
- 自查：写完函数，从上往下读"出口在哪"

### ④ 累加时分清「本轮的」和「累计的」
- 错例：`fp_all += tp_all - mc`（`tp_all` 是**累计**命中，`len(ps)` 才是**本轮**预测数）
- 这类错**不报错**，只把结果变成没有物理意义的数 → 最难查
- 自查：写每个 `+=` 前，用一句话说出"右边这一项是什么量"

### ⑤ 公式的分子分母必须语义匹配
- 错例：`R = fp_all / (tp_all + fp_all)` —— recall 的分子是"找到的"(`tp_all`)，不是"白报的"(`fp_all`)
- 更隐蔽的是：这个式子**仍在 [0,1] 内**（`fp/(tp+fp) = 1-P`），**不违反值域检查**，还可能算出 0.8 这种"看起来不错的 recall" → 会拿到一个**看起来合理但完全错**的 F1
- 自查：**先用一句话说出每个符号代表什么**；说不通，就不该写在那

### ⑥ import 一个模块 = 执行整个 .py
- **缩进为 0 的代码在 import 时无条件执行** → 测试代码必须放进 `if __name__ == "__main__":`
- 实例：`evaluate.py` 的模块级测试让 `train_eval.py` 一启动就多打印一行 `precision=0.67 recall=0.67 F1=0.67`（已修）
- ⚠️ 另一面：**改了磁盘文件对已启动的进程无效**（模块已读进内存）→ 改完必须重跑

### ⑦ 评估口径：人内匹配，禁止跨人配段
- 把 7 个测试者的段 `extend` 进一个大列表再算 P/R → **不同人的段会互相"配"上**（时间戳是真实时钟，多人同天采集常见）→ 虚报被别人的真值"洗白" → 分数虚高
- **正确**：逐人算 matched / tp / fp / fn，再累加（微平均）；或每人算 F1 再平均（宏平均）
- 待官方《测试接口说明》确认最终口径 → **评估函数单独成层**，换口径只动这一层

### ⑧ 参数只能按物理先验定，不能按 F1 最高定
- 反例：短段过滤阈值从 30s 试到 400s，F1 一路涨到 **1.0000** —— 因为只剩"恰好命中的那 2 段" = **用答案反推参数**（过拟合/信息泄漏）
- 正例：过滤 < 60s 的依据是"吃饭不可能只吃 1 分钟"（生理常识，不知道答案也能定）
- 判据：**这个参数，能不能在不知道答案的情况下定出来？**

---

## 已知缺陷（2026-09-21 复核 · 状态列以当时代码实测为准）
| 级别 | 问题 | 位置 | 状态 |
|---|---|---|---|
| P0 | **ACC 采集断续未处理**：存在小时级空档，跨空档滑窗会把相隔数小时的信号缝进同一窗口 → FFT/std 失真 | `src/preprocess.py` `sliding_window` | ❌ 未修（9-21 实测发现） |
| P0 | 训练/测试按窗口随机切分，相邻重叠窗口泄漏 → 准确率不可信 | `src/detect.py` `train_test_split` | ❌ 未修（**9-21 已定方案：按 zip 分组切分**） |
| P0 | **只用单人**：硬编码 `subject="HNU21026"`，官方给 44 人×多天 | `scripts/build_dataset.py` | ❌ 未修（detect_dominant 自测块同样硬编码） |
| P0 | **切分方式错误（2026-09-23 新发现）**：时间中点切分 → 训测场景不对等（换手成块） | `detect_dominant.py` `__main__` | ❌ 未修（**方案已定：改按受试者分组切分**，见上） |
| P1 | 只用 `ACC_X` 单通道，缺合幅值 mag 与陀螺仪 | `scripts/build_dataset.py` | 🔄 部分（`detect_dominant` 已改 **6 轴 30 维**） |
| P1 | 标签"有交集即 1"，边缘窗口噪声大 | `src/preprocess.py` `make_labels` | ❌ 未修（9-21 证实：还会**漏掉跨 zip 接缝的饭段**） |
| P1 | 缺官方要求的高斯平滑 + 180s 合并 | `src/segment_meals.py` | ❌ 未修（现仅做边界合并） |
| P1 | 评估非一对一匹配，precision 虚高 | `src/evaluate.py` `evaluate` | ❌ 未修 |
| P1 | 仅 5 个特征；主频未限 0.5–3 Hz 频段 | `src/features.py` | ❌ 未修 |
| P2 | `from notebooks.…import` 脏依赖 + sys.path typo(`workbuudy`，少个 d) + 未用的 `import pandas` | `src/detect.py` | ❌ 未修（用户要求先问再改） |
| P2 | `extract_imu_features` 空壳未决（与 `features.window_features` 功能重复） | `src/detect_dominant.py` | ⚠️ 待定 |
| P2 | docstring 说 `[mean,std,max,min,主频]` 但代码实为 `[mean,std,mn,mx,dom]`（max/min 反序） | `src/features.py` | ⚠️ 待定（影响读特征重要性） |
| P2 | starts 算法与 `build_dataset.py` 重复（DRY） | `src/detect_dominant.py` | ⚠️ 待定（9-28 统一） |
| — | ~~`from scipy.constants import precision` 会 ImportError~~ | `src/evaluate.py` | ✅ 9-17 实测未复现 |
| — | ~~采样率硬编码 105 Hz 是否准确~~ | `src/features.py` `ACC_FS` | ✅ **9-21 实测 ≈106.4 Hz，误差 1.3%，判定正确** |

## 官方时间线（2026-09-06 核实）
- **2026-07-28**：赛道通知发布（华为终端有限公司支持）
- **2026-08-21**：官方数据已发布到华为云研究平台
- ~~2026-09-20~~ → **2026-10-20**：线上报名截止（2026-09-20 官方公众号通知：报名延长一个月）
- **2026-09-30**：作品材料提交截止（源代码 + 设计报告）——⚠️ 延长通知未提是否顺延，暂按 9/30 准备
- **2026-10 中旬**：决赛名单 → 线下答辩

## 数据集 schema（赛题 PDF V1.1）
- 采集：健康人群（**训练集 40 人 + 测试集 20 人**）；连续 3 天非睡眠时段全天候佩戴；每日更换佩戴手腕；单人 ≥6 条有效用餐记录
- 核心数据源：**PPG（光电容积描记）+ ACC（加速度计）**
- 三张表（数据库）：
  - `t_zsstnnrj_sensororiginaldata_system`：PPG + ACC 原始信号（externalId = 受试者唯一 ID）
  - `t_zsstnnrj_mealinfo_puaddqoq7`：用餐信息（**beforeTime / afterTime = 饭段起止**；dietaryType / drinkingVolume / satiety / foodName / tablewareType；dietaryHand = 惯用手，wearHand = 佩戴手）
  - `t_zsstnnrj_userinfobean_5d4l0nmp`：用户基本信息（ID / 年龄 / 性别 / 身高 / 体重）
- 下载：需签《数据使用与保密承诺书》发 bmedesign05@hainanu.edu.cn 后到 https://research.cloud.huawei.com/researchportal/#/dataTable

## 评测机制（赛题 PDF V1.1 原文 · 2026-09-06 全文核读）
- **两种佩戴场景，分别设计算法**（PDF 原话"本质差异，需分别设计针对性算法"）：
  - 惯用手佩戴：进食手部动作特征明显 → **IMU**（加速度计+陀螺仪）运动模式检测
  - 非惯用手佩戴：动作特征弱 → **PPG** 生理信号（心率变化等）检测
- **主指标**：检测出的进食事件与真值 **IoU > 0.25** 判为正确 → 算灵敏度(recall) + 阳性预测率(precision) → **F1 为最终评价标准**
- **次指标**：正确命中事件的**开始/结束时间判断误差 MAE**
- 关键后处理（`segment_meals` 参考 `meal_detection_heuristic`）：高斯平滑 → 阈值 → 边缘检测 → 180s 合并

## 评分标准（PDF 原文 · 重要！此前档案遗漏）
| 得分项 | 内容 | 分数 |
|---|---|---|
| 结果准确性 | 测试集预测结果客观评分（IoU>0.25 的 F1）| **50** |
| 先进性和创新性 | 设计思路创新、原理科学先进、创新点清晰 | **40** |
| 总结报告与答辩表现 | 报告条理 + 答辩 PPT/陈述 | **10** |

## 提交要求（PDF 原文）
- 提交物：**最终算法代码（Python）+ 总结报告 + 可执行文件**（运行后生成测试集结果）
- 可执行文件无法运行 → **结果准确性 0 分**
- 训练代码 + readme（复现说明）备查，无法复现取消资格
- **官网仅能提交一次，提交后无法更改**——确认最终版再交
- ⚠️ **预测试机制**：初赛截止前一周（≈9/23）可将可执行文件发 bmedesign03@hainanu.edu.cn（文件命名 "answer_队伍名_赛题_提交日期"，邮件主题 "可执行文件测试_队伍名_赛题_提交日期"）→ 组委会测试给参考分 → 可迭代改进后再官网提交
- 报告：正文前言+引入 ≤2 页、方案/结果/讨论 ≤8 页、整体 ≤12 页
- 附件文档待取：测试接口说明（决定可执行文件怎么写）+ 异常数据说明

## 数据（PDF 原文 + 官网公告）
- 信号：**IMU（加速度计 + 陀螺仪）+ PPG**（华为手表采集，训练集 40 人 + 测试集 20 人，连续 3 天）
- 官网 2026-08-21 公告数据已发布；PDF 注"数据获取方法后续另行通知，报名后关注"
- 平台：https://research.cloud.huawei.com.cn/researchportal/#/dataTable

## 数据下载状态（2026-09-07 实测）
- 附件工具包已解压至 `进食检测比赛/华为数据下载工具/`（含下载脚本 + HiResearch SDK whl + DataTable.txt）
- SDK 已装到 envs/default（需 setuptools 补 distutils，Python 3.12+ 已移除）
- ⚠️ **下载被华为平台权限挡住**：所有查询返回 `HTTP 500 {"code":16001,"message":"User role does not permission to access"}`（匿名和带凭证都如此）
- 最可能根因：**数据访问权与报名/平台授权绑定**（PDF 原话"报名后关注数据获取"）；用户 9-20 报名截止前未报名
- 排查路径：① 确认报名（含指导教师）② 网页登录 research 平台核对凭证/项目 ③ 联系组委会 bmedesign03@hainanu.edu.cn 附 16001 报错
- 备选：DataTable.txt 已含两张表字段（sensor 表：externalid/sensorData/timeStamp；mealinfo 表 `t_zsstnnrj_mealinfo_puadqog7`：externalid/dietaryType/beforeTime/afterTime/.../wearHand）——注意 mealinfo 表名是 **puadqog7**（非 PDF 截图手抄的 puaddqoq7，已核对附件原文）

## 数据下载完成（2026-09-08 · ✅ 核心数据到手）
- **sensorData 原始信号 zip × 50**（PPG+ACC，~1GB）→ `data/raw/sensorData/`（✅ 全齐）
- **mealinfo 标注表 × 288 条**（44 人；午餐117/晚餐129/早餐27/加餐15；含 externalid/dietaryType/beforeTime/afterTime/foodName/wearHand）→ `data/raw/mealinfo_标注表.csv`（✅ 已导出）
- **diningPictures 用餐照片 × 47**（差 3，非训练必需，优先级低）→ `data/raw/diningPictures/`
- 重复副本 56 份已隔离 → `data/raw/_重复副本_待删/`（确认后可清，~400MB）
- 下载方式：**网页表内逐行下载按钮**（无批量；手动点出）；下载目录 `E:\浏览器下载` 已清空
- 关键认知：①"主表 vs copy 表"数据相同，别在 copy 重复下 ② 网页"导出"是表复制不是文件下载 ③ SDK 旧版 batch_download_file 卡 tableId，新版 hiresearchsdk 才有 get_attachment（未获取）
- UserInfoBean（受试者身高/体重/年龄）表尚未导出（可选补充）
- 下一步：按 `data/raw/mealinfo_标注表.csv` 的 beforeTime/afterTime 对齐 sensorData zip 内时间戳 → 训练窗口打标签

## 数据申请官方流程（2026-09-07 补充 · 来源：夸克扫描王_数据使用说明(1).pdf，浏览器下载目录）
- 流程：① 承诺书末尾填参赛队信息 → ② 负责人手写签名 → ③ 扫描件 PDF **+ 华为创新研究网站 ID** → 发 bmedesign05@hainanu.edu.cn → ④ **邮件标题必须 = 参赛队名 + "赛题名" + "数据使用申请"**
- 用户 14:41 已发首封邮件（格式不符但**已触发授权**：组委会有回信，凭账号 python2026 已能进"赛题二训练集"项目并浏览/导出数据表）
- 浏览器实际下载入口（2026-09-07 21:37 实测）：**左侧菜单"数据管理"→ "数据表"** → 列出所有表（MealInfo、传感器原始数据=SensorOriginalData、UserInfoBean 等）→ **点表名进入"Table列表"页** → 顶部按钮 **"选择字段 / 设置过滤条件 / 导出 / 导出记录"** → 设时间窗 2026-06-20 ~ 2026-08-13 → 导出 CSV
- 实战分工：**MealInfo 表（几百条）走网页导出**；**SensorOriginalData 表（千万行）走 SDK**——SDK 路线：网页"我的凭证"生成 access key → 替换下载脚本 429-430 行的两个值 → 跑
- 用户账号**已授权可用**（7-31 ~ 8-02 已有 10 条 MealInfo 记录可浏览），**不必再纠结补发邮件**

## 下一步（2026-09-23 重排 · 距 9-30 提交 7 天，其中军训至 9-27）

**已跑通**：数据下载 / data_loader / preprocess / features(v0.1) / detect(RF) / segment_meals(v0.1) / evaluate(v0.1) / **detect_dominant（6 轴全链路）** / **subjects.py（名册模块，9-23）**

**⚠️ 旧结论作废提示**：9-23 前的所有实验结果（含 IoU≈0.996、F1=0、4.74%/0.58%）**因切分方式错误而全部作废**，不可再作为基线引用。

**A 线（算法 · 按序执行，A-0 是共同地基）**：
- **A-0 多人数据 pipeline**（P0）：实现 `src/dataset.py` —— **按受试者逐个处理**（读一人→提特征→释放），用 `subjects.load_subjects()` 遍历 43 人；缓存按受试者分文件 `cache/<ID>.npz`
- **A-1 按受试者切分**（P0）：改成 Group Split by Subject（训练集的人 ≠ 测试集的人），**废弃时间中点切分**
- **A-2 按窗口/饭段筛惯用手**（P0）：配合 `is_dominant` 列表筛窗口，**不按 zip 筛**
- **A-3 跑出首个可信 F1 基线**
- A-4 特征增强：加合幅值 mag + 过零率/RMS/峰值计数；主频限 0.5–3 Hz
- A-5 后处理补全：高斯平滑 + 阈值 + 180s 合并；评估改一对一匹配
- A-6 改标签判定：`完全包含` → `有交集`（让跨 zip 接缝饭段生效）
- A-7 `detect.py` 三处残留清理（脏 import / sys.path typo / 未用 import）

**事务线**：
- **报名收尾**：推荐参赛表打印 → 黄老师签字 → 明德区才德楼 A301 盖学院章（0751-8121395）；官方截止 **10-20**（但作品材料按 9-30 准备）
- **预测试**：把可执行文件发 bmedesign03@hainanu.edu.cn（命名 `answer_队伍名_赛题_提交日期`）拿参考分

**提交物线（🔴 目前为零，最危险）**：
- **可执行文件**：⚠️ 官方明确「不运行 = 结果准确性 0 分」
- **设计报告**（创新性 40 分 + 报告 10 分）——可取用 `文件workbuddy/文件1/03_竞赛_进食检测/进食检测_技术发现与报告素材_假菌.md`
- **README 复现说明**

**文档线**：
- **PPG 分支**：推进队友鲁焯俊负责的非惯用手场景（`detect_nondominant.py` 仍是骨架），或自己破解 44 列 PPG 语义
