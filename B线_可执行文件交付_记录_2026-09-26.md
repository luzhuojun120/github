# B 线：可执行文件交付 —— 记录（2026-09-26）

> 本文是 `AGENTS.md` 的补充。**写本文时 `AGENTS.md` 被进程独占锁定无法写入**（见文末「环境问题」），
> 故单开此文件。待锁定解除后可合并回 AGENTS.md。

## 为什么做 B 线

官方提交要求原文：「可执行文件**无法运行** → 结果准确性 **0 分**」。
且「**官网仅能提交一次，提交后无法更改**」—— 交付物必须一次成型。

## 已完成（B-1 ~ B-6）

| 步骤 | 内容 | 验收结果 |
|---|---|---|
| B-1 | 路径统一（`src/paths.py`） | 17 处硬编码 → 0；10 模块 import 通过；回归输出逐字不变 |
| B-2 | 训练定型（`src/train_final.py`） | 37 人 / 802,038 窗口 / 50,400 正样本(6.28%) → `model.pkl.gz` 118MB |
| B-3 | 推理入口（`src/predict.py`，6 函数手写） | 端到端跑通，HNU21029 检出 40 段 |
| B-4 | 输出 CSV（UTF-8 with BOM） | BOM 验证 `ef bb bf`，对账通过 |
| B-5 | PyInstaller 打包（`--onedir`） | exe 与源码产出**逐字节一致**（205 bytes） |
| B-6 | 干净环境实测 | 全新目录：退出码 **0**、stderr 空、模型从 `_MEIPASS` 找到 ✅ |

## 🔴 打包踩坑记录（五条，勿重犯）

### 1. `sys.stdin.isatty()` 打包后不可靠 → 退出码 1

- **现象**：`subprocess` 调用 exe 时 `EOFError: EOF when reading a line` → `[PYI-xxxxx:ERROR]` → 退出码 1
- **根因**：PyInstaller `--onedir` 模式下 `sys.stdin.isatty()` 行为与普通 Python 不一致
- **风险**：评委若用自动化脚本调 exe，会拿到非零退出码 → 可能被判「运行失败」= 0 分
- **修法**：
  ```python
  if getattr(sys, "frozen", False):
      try:
          if sys.stdin.isatty():
              input("按回车键退出...")
      except Exception:
          pass
  ```
- **教训**：打包程序的「暂停等待」逻辑**必须 try/except 兜底**，不能只靠 `isatty()`。

### 2. PyInstaller 的 `datas` 落在 `_MEIPASS`，不在 exe 同目录

- **现象**：模型写进 spec 的 `datas` 后，出现在 `_internal/model.pkl.gz`，而 `paths.py` 找的是 `exe目录/model.pkl.gz` → `FileNotFoundError`
- **修法**：`paths.py` **双路径查找**：
  ```python
  _MODEL_CANDIDATES = [os.path.join(BASE, "model.pkl.gz")]
  if getattr(sys, "frozen", False) and hasattr(sys, "_MEIPASS"):
      _MODEL_CANDIDATES.append(os.path.join(sys._MEIPASS, "model.pkl.gz"))
  MODEL = next((p for p in _MODEL_CANDIDATES if os.path.exists(p)), _MODEL_CANDIDATES[0])
  ```
- **设计意图**：前者优先 = 允许现场替换模型；后者回退 = 内嵌可用。两个需求都满足。

### 3. `--onedir` 产物必须整个文件夹交付

- `predict.exe` **不能单独拷出来运行** —— 依赖同目录 `_internal/`（78→79 项）
- README 必须写明这一点，否则评委会「只拷 exe」然后报「无法运行」

### 4. sandbox safe-delete 会拦「覆盖已有文件」

- **现象**：`mv` / `open(p,"w")` / `Edit` 工具对已存在文件全部 `PermissionError`（被当作「删除+重建」）
- **修法**：**先 `mv` 旧产物改名走**，再写新文件（目标不存在则无覆盖）

### 5. 判断「乱码」必须抓原始字节

- **现象**：exe 输出在终端显示 `1 ¶Î`
- **实测**：`subprocess` 抓原始字节 = `b"1 \xb6\xce"` → `\xb6\xce` 是 **GBK 的「段」**
- **结论**：exe 输出是 GBK，GBK 终端显示完全正常 —— **非缺陷**，是读取端按 UTF-8 解读管道造成的假象
- **方法论**：多编码解码对比（GBK / UTF-8），不能只看终端渲染

## 交付结构

```
D://bme_submit//交付包//
├── predict/                    ← 整个文件夹交付
│   ├── predict.exe   (43 MB)   ← 双击运行
│   ├── _internal/    (79 项)   ← 运行库 + 内嵌 model.pkl.gz
│   └── test_data/              ← 空目录，评委放自己的 zip
├── 训练源码/                   ← src/*.py（15 个文件）
├── requirements_lock.txt       ← 122 个包锁版本
├── 使用说明_README.md
└── （待打分前删除）
```

**总计约 480 MB**。

## 环境证据（exe 自包含性）

- `_internal/python313.dll` + `python3.dll` ✅ 自带 Python 运行时
- `_internal/numpy/`、`pandas/`、`scipy/`、`sklearn/` 及各自 `.libs/` ✅ 依赖齐全
- **无需目标机器安装 Python**，无需联网

## ⏳ 待办（关键：等官方《测试接口说明》）

1. **官方「官网仅能提交一次」+ 附件「测试接口说明」未到**
   - 该说明决定：输入目录名、输出文件名/格式、时间预算
   - **当前打包是演练，真正定稿须等接口说明**（可能要改 `INPUT_DIR` / `OUTPUT_CSV` / 输出粒度）
2. **真正的「干净电脑实测」需用户手动做**
   - 沙箱内无法真实模拟「无 Python 的另一台机器」
   - 方法：拷 `交付包/` 到另一台 Windows 电脑（或另建系统账户）→ 双击 `predict.exe`
3. `predict_one` 粒度问题（逐 zip 跑 40 段 vs 合并跑 17 段）：**先不改**，等接口说明
4. 预测试提交：`answer_品食Panda_赛题二_YYYYMMDD.zip` → `bmedesign03@hainanu.edu.cn`

## 环境问题（阻塞记录）

- 2026-09-26 14:5x 起，`AGENTS.md` **被进程独占锁定**：
  - `open(p,"r+")` / `open(p,"a")` / `mv` 全部 `PermissionError`
  - 但 `open(p,"r")` 可读、`cp` 可读 → **读得到、写不了**
  - `attrib` 无 `R` 位、`icacls` 权限正常（Authenticated Users 有 M）→ **非文件属性问题，是运行时锁**
  - 等待 20 秒后仍锁定 → 疑似 WorkBuddy 文件监视器或编辑器缓存持有句柄
- **解法**：本次改写入新文件；待锁定解除后再合并回 `AGENTS.md`
