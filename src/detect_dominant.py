"""
detect_dominant.py
==================
惯用手场景检测（你主责）。

作用：
    手表戴在惯用手时，进食的手部动作明显，
    用 IMU（加速度计 + 陀螺仪）识别"手部运动模式"来判断是否在进食。

将来要写的函数：
    - extract_imu_features(window): 从一段窗口提取时域 / 频域特征
    - detect_dominant(window): 返回该窗口是否进食（True / False）

参考：
    公开数据集 UCI HAR 就是 IMU 人类活动识别，可以先照着学特征提取。
"""

# TODO: 实现 extract_imu_features
# TODO: 实现 detect_dominant
